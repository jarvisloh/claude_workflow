<!-- claude-workflow:begin -->
Use the explicit `cw-workflow` skill to select a route when the user asks for one. Light is the default. For substantive Heavy work, start one resumable Companion and dispatch an Archivist at closure; keep at most one Senior Executor active. Assign role-specific task capsules and verify worker reports before integration.

Project memory lives in `agent_docs/`. Preserve unrelated instructions and user-authored files. The installed agents live under `.claude/agents/`, skills under `.claude/skills/`, and runtime/reporting assets under `.claude-workflow/`; paths in this block are relative to the project root.
<!-- claude-workflow:end -->
